# TODO - task-management (branch homewrok 2)

- [x] Create `task-management` project folder and copy baseline structure
- [x] Convert entry + routes + controller to TypeScript under `src/`
- [x] Add SQLite connection + migration runner + tasks DB module
- [x] Refactor CRUD endpoints to use SQLite
- [x] Update `package.json` (name, scripts)
- [x] Add `tsconfig.json`
- [x] Run `npm install`
- [x] Start server (`npm run dev`) and verify `GET /api/tasks` returns []
- [ ] Add a reliable CLI/curl test command documentation (PowerShell quoting issues)
- [x] Ensure no leftover JS files conflict with TS build/runtime
- [x] Add repositories/ + services/ layers for Tasks

